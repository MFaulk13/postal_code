name: postal_code
description: Autocomplete field for postal codes all around the world
repository: https://github.com/Lamorak/postal_code/flutter_lib

version: 0.0.1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

flutter:
  uses-material-design: true
